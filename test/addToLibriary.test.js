const addBookToLibrary = require("../index.js");

test("addBookToLibrary is defined", () => {
  expect(addBookToLibrary).toBeDefined();
});

test("myLibrary is defined", () => {
  expect(myLibrary).toBeDefined();
});
